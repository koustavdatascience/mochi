# Local Clone Run

The generated application was created by running Ditto’s local in-memory API mode against `https://www.abhijithjinnu.in/`. No hosted Ditto API key was used: the local API was started with an empty `DATABASE_URL` and no `API_KEYS` configuration.

The clone job completed successfully with a captured DOM, generated Vite application files, copied media/font assets, and a successful local production build. A browser check of the local Vite preview confirmed that the navigation, hero, avatar, command-copy control, project carousel, experience records, and footer navigation render in the generated page.

The output intentionally retains reference-source content and assets because this is a direct capture result. Review ownership and licensing before distributing it beyond an authorized evaluation or reproduction context.

At the checked desktop viewport, the rendered document remained within the viewport width with no horizontal overflow. The command-copy control was also reachable and clickable in the local browser preview.

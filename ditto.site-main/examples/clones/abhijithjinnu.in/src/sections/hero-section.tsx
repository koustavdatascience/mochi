/** Hero section — the page's lead block. */
export default function HeroSection() {
  return (
    <div className="h-full block fixed top-0 inset-x-0 z-1000 min-w-0 overflow-hidden pointer-events-none" data-cid="n1" />
  );
}
